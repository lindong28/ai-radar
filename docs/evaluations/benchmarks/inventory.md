# 题库库存

## 2026-09-19 O1 时间 gold 重建（当前）

用户批准后，从冻结原始证据重建 `news-admission/aihot-observed-membership/v1`，主集 **1,450**（727 正、723 负），另有 **880** 题仅召回。未增加采集数据或 AIHOT-only 正例，不修改旧版本。其余对象仍为下节的评分 3,475、富化 12,006 字段题、精选 341 局部题。

本版处理 16,458 个原始身份、234,841 条输入变体、628 份 Radar 归档 manifest。主集 X 1,444、Web 6，共 94 来源；dev 1,156、regression 294。资格正负对称，不按 gold 重平衡。14,128 个身份排除：实质版本冲突 2,737、缺可靠原文历史下界 11,240、参照历史覆盖不足 122、实际到达晚于共同参照历史 29。缺可靠时间不等于文章无效，原始证据全部保留，后续有独立证据可再建下一版。

共同参照右界 H=`2026-09-19T00:07:06Z`；Radar 冻结轮次包络为 9/15 14:45Z–9/19 02:10Z，不表示其中所有候选均符合新主集资格。新主集主要覆盖 X，不能把新成绩外推到旧库全来源。相对旧主集，621 正例和 592 负例保持，3 个旧负例改正；另外 234 题从旧补充/排除身份进入主集。主集和补充集合计 2,330 题，合并变化为 added 136 / retained 1,280 / updated 914 / removed 9,790；removed 表示不再是本版评测题，不是删除原始数据。

数据位置及可重复构建命令见[本版说明](../news-admission/aihot-observed-membership/v1/README.md)。新 manifest SHA256：`f33fb137752f4a6d45611b0e70b59d2fd355d3f96e8c6d812aaaef2f4c9eb954`。历史版本保留复现，不相加。

## 2026-09-19 benchmark 身份迁移（O1 已被上节新契约替代）

已按消费者契约将下节四份旧命名题库发布到以下新 benchmark/v1。迁移不是扩题，不新增新闻或模型成绩，cases/reference 保持原字节；旧叶子及其历史指标仍可复现。

| 对象 | 新 benchmark / 版本 | 原叶子版本 | 主集题数 |
| --- | --- | --- | --- |
| news-admission | aihot-prefilter / v1 | aihot-all-members / 20260919-refresh-1010 | 11,804；另仅召回 180 |
| visible-score | aihot-score-pointwise / v1 | aihot-visible-score / 20260919-refresh-1010 | 3,475 |
| content-enrichment | aihot-enrichment-fields / v1 | aihot-enrichment / 20260919-refresh-1010 | 3,476 新闻 / 12,006 字段题 |
| featured-members | aihot-featured-threshold / v1 | aihot-featured-members / 20260919-refresh-o4-1010 | 341 局部题；完整候选组 0 |

题数已从四份源 manifest 核对，四个新叶子的实际 CLI validate 均完成；四份旧 manifest SHA 不变，cases 逐行相等，各叶子 files/evidence_files 摘要相等（O1 5/5,362，O2 4/5,362，O3 5/5,362，O4 4/685）。新 manifest.migration 绑定 source_dataset 与 source_manifest_sha256，不把目录发布时间补成历史采集时间。新版本完整路径为 `~/research/video-eval-arena/data/benchmarks/ai-radar/<target>/<新 benchmark>/v1/`。后续扩题显式以需要的这些叶子为 base，输出尚未使用的 v2；同名 v1 不表示不同 benchmark 共享同一证据。命名、迁移、合并去重及重验命令见[共用操作说明](object-datasets.md)。下方保留历次快照及当时的路径/命令，不作为当前建题默认；历史自由命名版本不能直接代入新 build 的 --version，按新规则改用下一 vN。

## 2026-09-19 10:10 +08 输入截止：合并新归档后重建（迁移源快照）

本轮复用 `0015e3b` 的建题代码及现有来源契约，没有修改 URL、实质版本或逐对象准入规则。O1/O2/O3 从 `20260918-substantive-v4-r1` 合并 Radar 已完成归档，并加入已通过原件校验的 AIHOT 9/15、9/18 UTC 日窗（分别 441、436 条参照记录），输出 `20260919-refresh-1010`。新增参照显式启用 `--aihot-inputs`，仅允许评分、富化使用绑定原文；准入仍从 Radar 未过滤输入建题。

| 对象／字段 | 当前总题数 | 相对上一正式版本净变化 |
| --- | --- | --- |
| 新闻准入主集 | **11,804**：1,379 正、10,425 负 | +3,731 |
| 准入仅召回补充集 | **180** | −17，不混入主集 precision |
| 可见评分 | **3,475** | +693 |
| 富化：分类 | **1,525** | +270 |
| 富化：标签 | **3,476**：1,493 非空、1,983 已观测空集合 | +693 |
| 富化：标题 | **3,476** | +693 |
| 富化：摘要 | **3,420** | +677 |
| 富化：推荐理由 | **109** | +14 |
| 精选局部阈值 | **341**：6 正、335 负 | +8；完整候选组仍为 0 |

富化覆盖 3,476 条新闻，共 12,006 道字段题；跨对象共 27,626 个主集评测任务，另有 180 个准入仅召回任务，不能将跨对象任务数称为独立新闻数。

### 新增、更新与移出

- O1 主集和补充集合计：新增 3,714、保留 8,076、更新 194、移出 0。原 197 条仅召回题中，192 条因新参照补足覆盖进入主集，5 条仍在补充集；另新增 175 条仅召回题，所以补充集为 180。主集净增 3,731 不等于新增身份数。仍有 4,474 个参照覆盖不足的未知身份，不当负例。
- O2：新增 693、保留 2,782、更新 0、移出 0。输入来源为 Radar raw 1,106、AIHOT 绑定原文 2,369。
- O3 按字段计：新增 2,347、保留 9,659、更新 0、移出 0。新闻输入来源为 Radar raw 1,107、AIHOT 绑定原文 2,369。
- O4 单独从 `20260918-substantive-o4-v4-r1` 合并新增 Radar 归档，沿用原参照窗口、不加入上述新日窗或 AIHOT-only 输入，输出 `20260919-refresh-o4-1010`。新增 11、保留 330、更新 0、移出 3；移出原因均为新证据暴露实质输入版本冲突，详见该叶子的 `changes.jsonl`。原窗口仍有 8 个身份缺同来源 raw、34 个实质版本冲突，本轮没有把局部题冒充完整池。

新 9/18 UTC 日窗另作完整池可用性核查：436 条参照中有 380 个共同来源身份，371 个配到 raw，9 个缺同来源 raw（Ars 4、Every 1、X 4），已匹配者另有 26 个实质输入版本冲突。因此尚不足以按既定保守规则扩充精选完整候选组；这不妨碍其中符合条件的单条评分／富化题入集。本轮只消费已收集证据，不补抓、不调整采集器。

### 版本、验证及后续扩展

题库仍位于本机 `~/research/video-eval-arena/data/benchmarks/ai-radar/<target>/<benchmark>/<version>/`。O1/O2/O3 的冻结证据合并后含 16,458 个原始来源／规范 URL 身份、628 份 raw manifest；时间包络为 `[2026-09-15T14:45:00Z,2026-09-19T02:10:00Z)`，不是连续采集证明。O4 独立证据为 14,663 个身份、628 份 manifest，不能与前者相加。

四份新叶子均已由真实 `load_dataset` 消费，核对冻结摘要、对象／路径／版本、唯一题目 ID 及建题／合并／身份模块摘要。覆盖 O1/O2/O3/O4 各 135/150/150/87 个来源，dev/regression 分别为 9,483/2,321、2,778/697、2,778/698、281/60；O1/O4 未混入 AIHOT 原文输入。只运行既有建题与资产校验，不改执行代码，因此不新增代码审查或重跑模型评测。L1 只更新②题集，L2 新增不可覆盖版本及变化清单，L3 规则不变；①③④⑤均未运行，没有新的拟合质量结论。

实际 CLI 输出保存在题库根 `_build-reports/20260919-refresh-1010/build-main.json`、`build-featured.json`；精确重建参数在各叶子 `manifest.rebuild`，变化在 `merge-summary.json` / `changes.jsonl`。旧版本保留供复现，不再作为当前题数。

后续 O1/O2/O3 使用 `--base .../news-admission/aihot-all-members/20260919-refresh-1010`，显式选择这三个 target；新增 AIHOT 原文输入时继续给 `--aihot-inputs`。O4 使用 `--base .../featured-members/aihot-featured-members/20260919-refresh-o4-1010 --target featured-members`，仍须单独判断新窗口完整候选组是否足够，不省略 target 混入零散参照。新增 raw 范围与 reference 的参数格式见[共用操作说明](object-datasets.md)，输出始终换新版本名，不覆盖当前或历史叶子。

## 2026-09-18：URL 身份与实质内容版本修复后重建

已按用户授权修复同来源 X URL 配对及逐对象实质内容版本判定。O1/O2/O3 使用 `20260918-refresh-2120` 的同一批冻结证据重建为 `20260918-substantive-v4-r1`；O4 单独从 `20260918-merged-v2` 的原窗口重建为 `20260918-substantive-o4-v4-r1`。没有新增抓取、没有模型调用，也没有运行模型质量指标。本轮变更是 L1②题目构建与 L3 身份／版本规则，L2 新增不可覆盖题库；①③④⑤未改未跑。

| 对象 | 当前总题数 | 相对原版：新增 / 保留 / 更新 / 移出 |
| --- | --- | --- |
| 新闻准入 | 主集 **8,073**：990 正、7,083 负；另有仅召回 **197** | 123 / 7,322 / 825 / 0（含补充题） |
| 可见评分 | **2,782** | 71 / 896 / 1,815 / 0 |
| 富化：分类 | **1,255** | 富化合计新增 258 字段题 |
| 富化：标签 | **2,783**：1,229 非空、1,554 已观测空集合 | 同下方字段合计 |
| 富化：标题 | **2,783** | 同下方字段合计 |
| 富化：摘要 | **2,743** | 同下方字段合计 |
| 富化：推荐理由 | **95** | 同下方字段合计 |
| 精选局部阈值 | **333**：6 正、327 负；完整候选组仍为 **0** | 220 / 113 / 0 / 0 |

富化共 2,783 条新闻、9,659 道字段题，变化合计为 258 / 3,303 / 6,098 / 0。跨对象共 20,847 个主集评测任务，另有 197 个准入仅召回任务；不能将它们称为独立新闻。O2 输入来源为 Radar raw 546、AIHOT 绑定原文 2,236；O3 分别为 547、2,236。O1 有 6,200 个参照覆盖不足的未知身份，仍不当负例。推荐理由仍只取真实非空参考，不能为了追平其它字段题量补造理由。

### 修复效果与保留边界

- 全部历史参照中，416 条旧准入负例纠正为正例，无正例变负例；新增 123 条仅召回补充题。之前点名的最新日窗 85 条错误负例均在这 416 条内，已全部纠正。主集总数不变不等于标签不变。
- 最新 AIHOT 9/17 UTC 完整日窗仍为 383 个共同来源身份；规范 URL 后 375 个有同来源 Radar raw，恢复配对 208 个，仅剩 8 个缺同来源输入。原文下方“216 个没有 raw”是旧 URL 配对算法的历史读数，不能继续引用为真实漏收数量。
- 剩余 8 个身份分布为 ars_ai 4、every_latest 1、anthropic_news 1、x_natolambert 1、x_aiatmeta 1；不跨来源替补、不伪造正文。本轮只修评测配对与版本规则，没有修改采集器。375 个有 raw 的身份中，按评分实质内容判据仍有 34 个冲突，因此不能仅凭配对率宣称精选全池完整。
- 先前移出的 11 条评分新闻中，5 条仅 published_at 变化、1 条仅 HTML 变化、1 条标题标点／来源标签变化，共 7 条均已恢复至评分和富化；4 条真实正文变化仍不入两对象。这些不是模型表现差的删题。
- O2/O3 原有题的已有参考字段逐一核对不变；updated 包括 URL／case_id 规范化、Radar raw 优先替换旧 AIHOT fallback 等输入变化，不是新增题或模型提升。两个对象各有 618 条历史题的 split 因统一 X 身份迁移；O1 为 0。未来对比必须同版重跑，不混用旧 split 或旧成绩。
- O4 新增 220 题仅来自原窗口中的 URL 配对及版本修复，没有加新窗口、AIHOT-only 输入或完整池。新题库仍只支持已声明的 pointwise-threshold 用途。

O1/O2/O3 的原始输入包络仍是 `[2026-09-15T14:45:00Z,2026-09-18T13:20:00Z)`，14,470 个来源/规范身份、495 份 raw manifest；包络不是连续覆盖证明。O1 主集来源 124 个，dev/regression 为 6,481/1,592；O2/O3 各 146 个来源，分别为 2,225/557 与 2,225/558。O4 沿原版 343 份 raw manifest 的证据，不以新批次覆盖扩大其窗口。

### 重建与后续扩展

题库根保持 `~/research/video-eval-arena/data/benchmarks/ai-radar/<target>/<benchmark>/<version>/`，原始抓取档案与历史题库不覆盖。实际 CLI 输出在同一根的 `_build-reports/20260918-substantive-v4/build-main-r1.json`、`build-featured-r1.json`，逐题变化仍在各叶子的 `changes.jsonl`；精确题数以 manifest.counts 为准。首构建的无 r1 版本保留；r1 将最终格式整理后的代码摘要同步到新 manifest，不覆盖已冻结版本。

```bash
PYTHONPATH=src:. uv run python scripts/build_eval_datasets.py build \
  --base ~/research/video-eval-arena/data/benchmarks/ai-radar/news-admission/aihot-all-members/20260918-refresh-2120 \
  --target news-admission --target visible-score --target content-enrichment \
  --version 20260918-substantive-v4-r1

PYTHONPATH=src:. uv run python scripts/build_eval_datasets.py build \
  --base ~/research/video-eval-arena/data/benchmarks/ai-radar/featured-members/aihot-featured-members/20260918-merged-v2 \
  --target featured-members --version 20260918-substantive-o4-v4-r1
```

上面是本轮已运行参数的主仓用法，版本已存在不能覆盖；重新执行时换新版本名。未来 O1/O2/O3 从 `20260918-substantive-v4-r1` 继续 `--base`，按需加新 raw 和 reference；O4 从其独立版本沿连续完整候选组边界处理，不省略 target 让其它对象的零散参照自动扩入精选。旧版本保留是供审计／复现，不表示旧错误标签仍属于当前正式题集。

验证：233 项 eval 离线测试通过，覆盖四对象、schema1/2 迁移、来源/URL 别名、非实质与真实内容变化、参考冲突、重复版本合并及原文 fallback。新增身份测试 15 项含 8 类外观变化、2 类实质变化、6 对数值/运算符反例、4 种 X URL；独立审查发现的数值归一化、句末句号和连续感叹号问题均修复、定向复核关闭。连续感叹号修复触及 review 修复轮预算后，用户通过 AskUserQuestion 明确授权“继续定向修复”。这些验证不代表模型拟合质量已经提高。

四份 r1 正式叶子均经 `load_dataset` 重新读取，资产摘要、路径、题数及最终建题／合并／身份／原文提取代码摘要逐项匹配。r1 与首构建的 cases、excluded 及适用的 recall-only/field-subsets 逐字节相同；仅用首构建新版本 `--base`、不提供原采集目录的真实 CLI 复建也逐字节相同，O1/O2/O3 的新增、更新、移出均为 0。复建产物在上述 `_build-reports/.../replay/`，不是另一份应累加的题库。

新 evidence 的 186,108 条不同原始载荷及各自 provenance 与原版逐条摘要多重集相等；只有 2 个原始身份 key 被 URL 规则迁移，不能将 evidence 文件整体摘要变化误读为原文被改。四份旧叶子的 manifest/cases 共 8 个摘要在重建前后不变。后续 session 直接使用当前 r1 版本，无需消费构建验证目录。

## 2026-09-18 21:20 +08 输入截止：按当前充分性口径更新

本轮依据用户“基于收集到的数据更新每个评测对象的题目集”的要求，使用已提交的 `572f6ee` 建题代码及当前来源契约，完成 O1/O2/O3 的②题集重建，版本 `20260918-refresh-2120`；O4 核查后未扩展，原因见下。不运行①推理、③判官或④质量指标，⑤本轮未跑，不改变 L3 规则。这不是新的模型拟合成绩。

输入合并 `20260918-merged-v2` 与 `20260918-aihot-original-v3` 的冻结证据，加上 Radar raw run 起始时间 `[2026-09-15T14:45:00Z,2026-09-18T13:20:00Z)`；只消费已完成、校验有效的归档，不等待进行中的轮次。AIHOT 沿用已完成的 9/05—9/14、9/17 UTC 日窗及 9/17 07:00—12:00 UTC interval。本轮不产生新的抓取。原始来源/URL 去重、内容版本冲突与逐字段判定均沿现有脚本，O2/O3 的 AIHOT 原文权限由 base 继承，不将这些输入混入 O1/O4。

| 对象 | 更新后总题数 | 相对 base 去重并集：新增 / 保留 / 更新 / 移出 |
| --- | --- | --- |
| O1 新闻准入 | 主集 8,073（574 正、7,499 负）；另有仅召回 74 | 570 / 7,563 / 14 / 0（含补充题） |
| O2 可见评分 | 2,711 | 8 / 2,685 / 18 / 11 |
| O3 内容富化 | 2,711 条新闻，9,401 字段题 | 24 / 9,306 / 71 / 46（字段题） |
| O4 精选规则 | 保留 `20260918-merged-v2` 的 113 道局部阈值题（3 正、110 负）；本轮新增完整候选组为 0 | 本轮不重建、不新增 |

O3 分别为：分类 1,218、标签 2,711、标题 2,711、摘要 2,675、推荐理由 86。评分从 2,714 净减 3，富化从 9,423 净减 22；移出的 11 道评分题和 46 道字段题均由 `changes.jsonl.reasons` 与 `excluded.jsonl` 确认为原始／参考版本歧义，未按模型表现删题。O1 新增主集 564、补充集 6，另有 14 条从仅召回转为主集，因此主集净增 578，不能把 578 称为全新新闻数。

新 evidence 保留 14,470 个 Radar 来源/URL 原始身份（此前 10,830）、495 份 raw manifest；失败／错误记录 8,608 条，既不是缺失新闻数，也不是不可用题数。O1 主集覆盖来源从 83 增至 124，O2/O3 各从 133 增至 135；dev/regression 分别为 O1 6,481/1,592、O2/O3 各 2,179/532。O1 最早新闻发布时间扩至 `2026-08-31T20:36:53Z`，O2/O3 仍为 `2026-09-03T11:00:00Z`；这些是新闻时间，不是连续抓取的起点。准入另有 6,323 个原始身份因逐题 AIHOT ±12h 参照覆盖不足保持未知，不当负例。

O4 按完整候选组要求另行核查：最新已完成的 AIHOT 9/17 UTC 日窗内有 383 个共同来源新闻身份，在本轮冻结 Radar 原始池中仅 167 个有配对，216 个没有 raw；有配对的 167 个中另有 57 个原始多版本。此读数来自该日窗原件、当前来源契约及新 evidence/raw-inputs.jsonl 的同来源/URL 对齐，不是由失败轮数推断缺新闻，也不把缺 raw 算作 prefilter 假阴性。现有原件不足以据此构造该窗可靠的完整候选组；因此不把零散配对题扩作精选全池题。已有 113 道 pointwise-threshold 题作为原冻结版本保留，不宣称已用新证据重验或代表完整池；历史 v1 五小时实验也不冒充本轮新增。O4 的扩展受所需原始候选及版本证据缺口限制。

本轮三份新题库和沿用 O4 叶子都已通过项目 `load_dataset` 冻结摘要、路径、身份与计数校验；它只证明这些资产符合各自声明的版本，不证明对象质量或整个采集过程连续。旧版本及历史成绩不覆盖；不以固定 dev/regression 划分宣称这些新闻从未参与过任何历史调参。

复用实际命令（在 AI Radar 根运行，换新版本名及后续输入截止时间；题库仍在本机，不用 DGX）：

```bash
PYTHONPATH=src:. uv run python scripts/build_eval_datasets.py build \
  --base ~/research/video-eval-arena/data/benchmarks/ai-radar/news-admission/aihot-all-members/20260918-merged-v2 \
  --base ~/research/video-eval-arena/data/benchmarks/ai-radar/visible-score/aihot-visible-score/20260918-aihot-original-v3 \
  --raw-root /Users/lindong/research/ai-radar/data/raw-capture \
  --start 2026-09-15T14:45:00Z --end 2026-09-18T13:20:00Z \
  --target news-admission --target visible-score --target content-enrichment \
  --version 20260918-refresh-2120 \
  --contract-path /Users/lindong/research/ai-radar/tests/fixtures/aihot_sources.json
```

下一次 O1/O2/O3 扩展只需以新版本任一叶子为 `--base`，即可继承本批三个对象及自包含的全部证据；新增原始范围和已完成 AIHOT 参照按[共用说明](object-datasets.md)追加。O4 沿其原版本与完整候选组要求处理，不从上述命令省略 `--target` 来默认混入零散精选题。

## 2026-09-18：仅评分与富化接入 AIHOT 历史原文

已实际运行离线建题 CLI，以 `20260918-merged-v2` 为 base，加入 9/05—9/14 共 10 个已完成 UTC 日窗，显式 `--aihot-inputs` 授权当次全部参照（包含 base 继承的参照）作为 O2/O3 原文候选，仅输出这两个对象的 `20260918-aihot-original-v3`。新版本路径为 `~/research/video-eval-arena/data/benchmarks/ai-radar/<target>/<benchmark>/20260918-aihot-original-v3/`；未重新采集、未调用模型，不是新的拟合成绩。

| 对象 | 当前使用版本 | 题数 | 相对 base：新增 / 保留 / 更新 / 移出 |
| --- | --- | --- | --- |
| O1 准入 | `20260918-merged-v2`，本次不改 | 主集 7,495（419 正、7,076 负）；仅召回 82 | 本次未重建 |
| O2 评分 | `20260918-aihot-original-v3` | 2,714 | 2,601 / 113 / 0 / 0 |
| O3 富化 | `20260918-aihot-original-v3` | 2,714 条新闻、9,423 字段题 | 9,029 / 394 / 0 / 0（字段题） |
| O4 精选 | `20260918-merged-v2`，本次不改 | 113（3 正、110 负），只支持局部逐条阈值 | 本次未重建 |

O3 各字段为分类 1,226、标签 2,714、标题 2,714、摘要 2,681、推荐理由 88。O2/O3 均由 2,466 条 AIHOT 绑定原文输入和 248 条 Radar raw 输入组成；新增评分 2,601 不等于新增 AIHOT 原文 2,466，因为扩充参照还使既有 Radar raw 获得可用参考。上述数量直接读取各叶子的 manifest.counts 和 merge-summary.json，不将不同版本题数累加；两个对象共用新闻也不重复称为独立新闻。

两对象各覆盖 133 个来源，dev/regression 为 2,181/533。最早新闻发布时间是 `2026-09-03T11:00:00Z`（+08：9/03 19:00），最早输入观察时刻是 `2026-09-07T08:27:48.064048Z`（+08：9/07 16:27:48）；其中 2,388 条新闻发布时间早于 9/15 UTC。所加日窗按 `aihot_timeline_v1` 归属而非新闻发布时间，所以 9/05 起的参照窗含更早发布的新闻并不矛盾；这些不是“9/03 已开始持续抓取”的证据。

两对象各有 475 条排除记录：缺原标题 334、原始/参考版本歧义 84、缺真实正文 57。非空缺正文提示不作为原文；输入只取身份绑定的文章/推文正文，AIHOT 改写标题、摘要与其他答案保持为参考输出。题量增加没有放宽 O1 正负样本分布或 O4 连续完整候选池要求。

冻结证据仍保留 10,830 个 Radar 原始来源/URL 身份、343 份 raw manifest 与 5,162 条来源失败/错误记录，后者不是漏轮数。manifest.window 仍为 Radar 选取时间 `[2026-09-15T14:45:00Z,2026-09-18T00:00:00Z)`，不表示 AIHOT 历史题的时间跨度，也不能证明连续采集。输入来源与三种时间口径、重建/扩展和授权继承见[共用操作说明](object-datasets.md#仅扩充评分与富化aihot-原文输入)。

实际新版本通过 loader 完整校验后，仅给新版本 `--base`、不传 `--aihot-inputs` 或原始 `--reference`，经真实 CLI 重建两对象，cases.jsonl 与 excluded.jsonl 均逐字节相同。该验证覆盖 133 个来源、2,714 个不同 content_hash 的新闻；重建变化为 O2 retained=2,714、O3 retained=9,423，其余变化为 0，model_calls=0。O1/O4 旧叶子的 manifest/cases 四个摘要构建前后不变。

4 个测试文件的 58 个离线测试通过，覆盖 4 对象、schema1/2、真实正文/占位提示/缺失/冲突、Radar 优先和 base 自足重建；独立实现审查及缺正文问题定向复核关闭。上述验证只证明本轮建题、隔离及重建链路，不证明模型质量、所有原站全文完整或持续采集无断档。

后续 O2/O3 扩展从本次新版本继续 `--base`；O1/O4 仍沿各自旧版本与保守建题规则，不以 O2/O3 新版充当完整候选池。旧题库及历史记录保持原样；下文按各次构建时点阅读。

## 2026-09-18：历史版本合并、去重、按当前规则重验

已实际运行 `scripts/build_eval_datasets.py build`，将 `20260917-0700-1200-v1`、`20260918-object-v2`、`20260918-object-v2-r1` 三版作为 `--base`，输出 `20260918-merged-v2`。路径为 `~/research/video-eval-arena/data/benchmarks/ai-radar/<target>/<benchmark>/20260918-merged-v2/`。这是未来扩展的现有 base；完整用法见[共用操作说明](object-datasets.md)。未添加新的采集数据、未调用模型。

| 对象 | 合并后当前题数 | 相对三版历史并集：新增 / 保留 / 更新 / 移出 |
| --- | --- | --- |
| O1 准入 | 主集7,495（419正、7,076负）；仅召回82 | 0 / 7,371 / 206 / 0（含补充题） |
| O2 评分 | 113 | 0 / 111 / 2 / 7 |
| O3 富化 | 113条新闻，394字段题：分类58、标签113、标题113、摘要107、理由3 | 0 / 387 / 7 / 23（字段题） |
| O4 精选 | 113 | 0 / 93 / 20 / 1,139 |

当前正式评测任务共8,115，另有82个准入召回补充任务；不是8,197条不同新闻。这次 added 均为0；与 v2-r1 相比，四对象主集的输入和参考逐条均相同。updated 表示历史多版之间有状态差异，并不表示这轮修改了 v2-r1 的参考答案。

旧 O4 的实际执行池还含大量 `member=false, featured=false` 负例，旧指标确实消费这些记录。因此不能把“历史可见新闻的配对数”当作旧 O4 的全部题数：三版 O4 历史身份并集为1,252，按当前规则只留下113；移出1,139中，1,119缺当前可用精选配对参考，20存在原始/参照多版本歧义。O2的7题、O3的23字段题也因多版本歧义移出。逐题依据在各叶子的 changes.jsonl；不是因为模型表现差而删题，也不删历史版本。

合并证据含10,830个来源/URL原始身份、343份raw manifest、5,162条去重后的失败/错误记录；保留旧v1的采集失败信息。输入时间包络仍为 `[2026-09-15T14:45:00Z,2026-09-18T00:00:00Z)`，不证明连续无断档。重叠紧凑归档不能还原精确抓取总次数，raw_observations 为 null，不把不同版本旧计数相加。

真实 CLI 生成后重新 load 四对象并校验全部冻结证据；三版12个旧叶子的 manifest/cases 摘要前后相同，旧证据重验也通过。相关离线测试187项通过，新增6个测试函数覆盖重复版本、祖先重入、schema1/2、4对象/5富化字段、增量raw、字段冲突、暂停来源、原采集目录缺席、旧负例/失败迁移、损坏证据和CLI错误路径。独立审查的两项迁移统计缺陷已修复并定向复核关闭。此处只验收建题和扩展流程，不是模型质量或持续采集验收。

## 2026-09-18：逐对象独立题库

已使用 [object-specific-v2 建题脚本](object-datasets.md) 实际生成并重建版本 `20260918-object-v2-r1`，位于本机 `~/research/video-eval-arena/data/benchmarks/ai-radar/<target>/<benchmark>/20260918-object-v2-r1/`。输入为 Radar run 起始时间 `[2026-09-15T14:45:00Z,2026-09-18T00:00:00Z)`（57小时15分钟），参照为已有 9/17 07:00—12:00 UTC 小时窗和已完成 9/17 UTC 日窗。精确源路径/参数在各 manifest.rebuild；本轮没有重新抓取参照，也没有模型调用。

首构建 `20260918-object-v2` 也保留；r1 使用相同内容的主仓来源契约路径，避免重建参数指向临时开发 worktree。两次四对象的 cases、子集与排除记录逐文件摘要相同，已通过真实重建验证；扩窗增题另有生产格式 fixture 覆盖。旧首构建若复用 manifest.rebuild，需将 contract_path 改为主仓 `tests/fixtures/aihot_sources.json` 并核对冻结契约内容。

脚本识别91个在所给 AIHOT 参照实际出现的共同来源，读取343份已校验 raw manifest（计入304引用依赖），839,013条抓取观察，按来源/URL去重10,830条原始新闻。失败/未出现的来源-轮次记录5,161条留在 evidence/inventory.json；这不是漏轮次数，也不证明57小时连续无缺口。输入只排除来源范围之外的数据，没有先经过 prefilter 或评分。

| 对象／子集 | 本版题数 |
| --- | --- |
| O1 主集 | 7,495：419正例、7,076负例 |
| O1 仅召回补充集 | 82正例，不并入主集 precision |
| O1 未知 | 3,253条，AIHOT逐题±12h参照覆盖不足，保留原始输入、不作负例 |
| O2 可见评分 | 113 |
| O3 分类／标签／标题／摘要／推荐理由 | 58／113／113／107／3；标签含59条已观测空集合 |
| O4 精选成员 | 113：3正例、110负例；只支持逐条阈值，不是完整TopK池 |

O2/O3/O4 各有53条原始/参照多版本歧义、217条缺raw配对记录；没有猜正文或补造参考。O3各字段的分母独立，113条新闻并不代表每字段都有113题。O2/O3/O4题数相同是这批数据恰好同时有分数、标签和精选布尔，非代码要求三对象取交集。

四份真实题库均经 loader 重新读取并校验冻结摘要；dev/regression 分别为 O1 6016/1479，其余对象89/24。旧版O1仍可读，243题不变。建题代码及既有评测回归共181项通过；本轮新增测试覆盖4对象、5富化字段、schema1/2、正/负/未知、同/异标签集合、多版本、扩窗新版本、304有效/损坏及四类正文分隔符；这不是模型质量通过。独立审查发现的旧相对路径兼容与标签换序两项已修复、复核关闭。

本轮交付的是题库与复用脚本。v2逐对象推理适配由后续评测/优化任务接续，旧全池runner拒绝v2；当前评分/精选网站算法未改，也没有新增模型成绩。仅3条理由题与3条精选正例不足以推断广泛拟合效果。

## 历史 v1（保留）

> [Developer] · 2026-09-17 当前实施快照；冻结后的 manifest/counts 为数量权威，不把抓取次数当题数。

首批候选输入窗：`[2026-09-17T07:00:00Z,12:00:00Z)`，五小时。已实际取得 AIHOT 71 条参考、27 个显示来源；71 条可见分，26 条非空标签、45 条明确空标签；该窗精选及推荐理由均为零。字段完整不代表已完成来源配对。

配对前的真实核查暴露“抓取时间 vs AIHOT timeline”不一致：至少88条旧的已收录新闻会被旧算法误标负例。用户已裁决 O1 改为新闻生成时间 ±12 小时匹配。

已冻结版本 `20260917-0700-1200-v1`，位于本机 `~/research/video-eval-arena/data/benchmarks/ai-radar/` 下四个 benchmark/target 叶子。共同来源22个，原始抓取观察51,003条，去重后的未过滤新闻1,187条；同窗AIHOT参照61条，其中34条有raw、27条缺raw。后者不进入O1题集，但保留在其它对象原始参考池，评分与富化只取有raw的对应字段题。

| 题集 | 实际可计题数 |
| --- | --- |
| O1 prefilter | 243：107正例、136负例；其余944条unknown不计分，361条Radar覆盖不足、583条AIHOT覆盖不足 |
| O2 可见评分 | 34对 |
| O3 分类／标签／标题／摘要／理由 | 18／34／34／32／0对 |
| O4 精选 | 本窗口参考精选0条，召回率无分母，不得称已验证精选拟合 |

O1的24小时证据可以超出五小时输入窗，所用额外归档一并冻结；243不是原始新闻总量。来源排除依据机器契约的 `wechat_only` 和 `ai_radar_main_timeline_member`，不凭 `wx_` 名称推测。旧设计中前缀排除的描述在此被显式字段判定替代。

新版本统一用 CLI build 注册到四个 benchmark 叶子，来源原件、原始全观察、题目SHA与引用可沿 manifest 查回。O1待定条目保留在原始资产中，随着未来完整覆盖可在新版本重新判定；不是删除新闻。
