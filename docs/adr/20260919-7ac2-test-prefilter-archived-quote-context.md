# 离线接入已归档的引用帖上下文

状态：用户批准，独立决策评审放行（2026-09-19）。仅离线实验，不改生产。

用户目标仍是准入 precision 与 recall 均 >90%；本轮明确答复“允许加入原始引用上下文”。只取现有原始归档，不使用 AIHOT 改写或标签，不新增外部补采，保持 gold，剩余调用上限413。

在显式候选中，按 X post ID 取单跳 quoted 关系；引用 raw 必须在题目原始观测时点前已归档，无实质正文冲突。仅原始标题、正文、作者、URL进入独立引用段，正文最多4000字符。缺失/冲突不注入、不删题，不递归、不把 reply 当 quote。原始 cases 保持不变，逐题上下文及来源/时点写入 run sidecar，参与对象身份与缓存复用校验。基线是 C7 原输入，候选只增加上下文。

依据：新169题回归 C7+规则为 TP67/FP9/FN12/TN81，P88.16%/R84.81%；12FN中9条缺引用正文，冻结池仅2条有此前已归档的引用。若只修好这2条而其余不变，R仅69/79=87.34%，不承诺本增量足以双90。证据：runs/news-admission/aihot-observed-membership/v1/2026-09-19/15-00-28/quote-context-audit-corrected.json。原始池188,424,730字节，一次读取约0.6秒，引用构建不增加网络调用。

保持输入只改prompt未被证伪，用户本次选择测试上下文；全库运行、外部抓取和改gold不在范围。先在已见169题作诊断，不能再称未见；如值得继续，冻结候选后从从未参加旧开发/回归的dev另选100题作为本次holdout，保留原split并明确实验角色。最多169+100+100=369首试，余44恢复；失败或无增益可提前收口，预算不是必须花完。单臂独占API批并发8，两臂并跑则各4，总8；每题90秒deadline、无SDK重试，批次有限题数完成后退出。

只影响离线 news-admission。20260919-d62b固定输入是旧C6-C8轮的条件，本次后续明确许可不改旧实验；20260919-c31a的三个确定性条件继续保持。相关决策及当前time-label-design已核对，无历史覆盖或全来源外推。

独立reviewer /root/prefilter_candidate_decision 在HEAD0d13fee只读审查七项判据，全部成立，放行；唯一提示是上述条件性召回上界，无blocker/应修/承重未知。主线程负责真实结构、时点、身份、未见选题与预算验证，模型收益由真实实验判定。新增验证只覆盖用户要求的原始上下文与既有评测完整性边界，不加新治理层。
