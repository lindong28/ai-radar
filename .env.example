# ── LLM Provider Keys (at least one is required) ──────────────

# DeepSeek (used for prefilter / scoring / enrichment)
DEEPSEEK_API_KEY=sk-xxx
# DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# Volcengine ARK platform (alternative DeepSeek backend)
# ARK_API_KEY=xxx
# ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3

# OpenAI (used by codex_gpt_mini scorer)
# OPENAI_API_KEY=sk-xxx

# GLM (used by glm prefilter provider)
# GLM_API_KEY=xxx

# ── Provider Selection ────────────────────────────────────────

# AI_RADAR_PREFILTER=deepseek_v32
# AI_RADAR_SCORER=deepseek_v4_pro
# AI_RADAR_ENRICHER=deepseek_v4_pro

# ── Tuning ────────────────────────────────────────────────────

# AI_RADAR_DB=data/radar.db
# AI_RADAR_ENRICH_WORKERS=1
# AI_RADAR_DEEPSEEK_TIMEOUT=90
# AI_RADAR_OPENAI_TIMEOUT=60
# AI_RADAR_OPENAI_SCORER_MODEL=o4-mini
# AI_RADAR_FORCE_HEURISTIC=false
# AI_RADAR_ALLOW_LLM_FALLBACK=true

# ── Content Sources ───────────────────────────────────────────

# Mp2RSS feed for WeChat Official Account ingestion (required when the wechat
# source in data/sources.toml is enabled). The URL embeds a private key, so
# keep it out of git: put the real value in `.env`, `~/.claude/.env`, or the
# environment used to run the pipeline. Referenced as ${MP2RSS_FEED_URL}; the
# pipeline fails fast at startup if the wechat source is enabled but this unset.
MP2RSS_FEED_URL=https://www.mp2rss.com/feed/xxx

# ── Monitoring / Alerting (optional) ──────────────────────────

# Feishu custom-bot webhook used by `./run.sh admin alert-check` and the
# `alert` launchd service. Keep this secret out of git; put the real value in
# `.env`, `~/.claude/.env`, or the environment used to run `./install.sh alert`.
# FEISHU_GENERAL_ALERT_WEBHOOK=https://open.feishu.cn/open-apis/bot/v2/hook/xxx

# ── Eval (optional) ──────────────────────────────────────────

# AI_RADAR_EVAL_REPEAT_BORDERLINE=false
# AI_RADAR_DEEPSEEK_JUDGE_MODEL=deepseek-chat
# AI_RADAR_EVAL_MAX_TOKENS=2048
